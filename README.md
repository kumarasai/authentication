Firstly install npm and create project using npm create-react-app react_project
Create Firebase account and make some requirements in authentication tab and copy the api and required info to our app by importing firebase and required packges
then Simply run in your machine using the command "npm install"
Start application by using "npm start"